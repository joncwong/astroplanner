import React from 'react';
import Paper from '@material-ui/core/Paper';

const scheduleStyle = {
    marginTop: '20px',
    marginRight: 'auto',
    marginLeft: 'auto',
    marginBottom: '30px',
    width: '80%',
    height: '500px'
}

const Schedule = (props) => (
    <Paper style={scheduleStyle}>
        <p>hello</p>
    </Paper>
)

export default Schedule;
